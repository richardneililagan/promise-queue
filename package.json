{
  "name": "promise-queue",
  "author": "Richard Neil Ilagan <me@richardneililagan.com>",
  "description": "Throttle the execution of multiple concurrent Promises.",
  "version": "0.0.1",
  "license": "MIT",
  "main": "lib/index.js",
  "keywords": [
    "promise",
    "queue"
  ],
  "babel":{
    "presets" : ["es2015"]
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-preset-es2015": "^6.3.13"
  },
  "scripts": {
    "build": "babel src --out-dir lib --ignore *.import.*",
    "prepublish": "npm run build",
    "watch": "babel -w src --out-dir lib --ignore *.import.*",

    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
